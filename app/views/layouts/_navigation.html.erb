<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Assembler", root_path, class: "navbar-brand", id: "logo" %>
    </div>
    <% if is_logged_in %>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <% if user.superuser? %>
              <ul class="nav navbar-nav">
                <li><%= link_to "Manage Users", admin_users_path %></li>
                <li><%= link_to "Manage Tests", programs_path %></li>
              </ul>
          <% end %>
          <ul class="nav navbar-nav navbar-right">
            <% if user.superuser? %>
                <li><%= link_to "Take Test", tests_path %></li>
            <% else %>
                <li><%= link_to user.name, tests_path %></li>
            <% end %>
            <li><%= link_to "Logout", access_logout_path %></li>
          </ul>
        </div>
    <% end %>
  </div>
</nav>

